name: Python Integration Tests

on: [push, pull_request]

jobs:
  test:
    # Explicitly use the latest ubuntu runner
    runs-on: ubuntu-latest
    
    # Add this node-version hint to satisfy the runner requirements
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          node-version: 24 # Explicitly map to the new standard

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Run Tests
        env:
          # We inject the OpenAI Key from GitHub Secrets
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          # Path for Chroma to use during the test
          CHROMA_PATH: ./test_chroma_db
        run: |
          pytest tests/test_rag_pipeline.py -v